请审计下面给出的C/C++函数，判断函数代码本身是否存在安全漏洞。

只能根据函数代码中的数据流、边界条件、对象生命周期、整数运算、危险操作和控制流进行判断。
不得根据样本编号、项目名称、数据集标签或外部知识猜测答案。
分析过程采用静态审计，但“静态审计方式”本身不是判断代码安全或不安全的理由，reason中不得复述任务规则。

请严格只输出一个JSON对象，不要输出Markdown代码围栏，也不要输出JSON之外的解释。

JSON必须包含以下五个字段：

- vulnerable：布尔值，存在足够代码证据时为true，否则为false；
- cwe：根据代码证据填写"CWE-数字"，无法可靠确定时为null；
- location：函数内从1开始的相对行号数组，只填写与漏洞根因直接相关的行；
- confidence：0到1之间的自报置信度，仅表示模型主观把握，不代表经过校准的概率；
- reason：简短说明关键数据流、危险操作、生命周期错误或缺少的检查。

额外约束：

- 不提供任何具体CWE示例，必须独立判断漏洞类别；
- 不得默认所有内存安全问题属于同一种CWE；
- vulnerable为false时，cwe应为null且location应为空数组；
- vulnerable为true但无法可靠确定CWE时，允许cwe为null；
- location中的行号不得重复，也不能超过函数总行数；
- reason不得编造代码中不存在的变量、函数或控制流；
- 证据不足时必须保守返回vulnerable=false，并在reason中说明证据不足。

样本元数据：
{metadata}

函数代码：
```c
{code}
```
